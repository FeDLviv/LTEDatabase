XAML (разметка)

BINDING (Source, ElementName, Path, BindingMode, UpdateSourceTrigger, TargetNullValue, RelativeSource, ValidationRules, ValidatesOnExceptions, ValidatesOnOataErrors, NotifyOnTargetUpdated, NotifyOnSourceUpdated, StringFormat) 
	    (IValueConverter) 
	    (CompositeCollection, MultiBinding, PriorityBinding)
	    (привязка)

DEPENDENCYPROPERTY (INotifyPropertyChnaged, DependencyObject) 
				   (свойства зависимости)

ROUTEDEVENT (маршрутизируемые события)

TRIGGER (Trigger, MultiTrigger, DataTrigger, MultiDataTrigger, EventTrigger)
		(триггер свойств, триггер данных, триггер события) 

COMMAND (ICommand)

STYLE (x:Key, TargetType, BasedOn)

RESOURCES

CONTROL(ContentControl, ItemsControl) 
	   (однодетные элементы управления, многодетные элементы управления, диапазонные элементы управления, все остальное)
	   
TEMPLATE

RESOURCEDICTIONARY

ANIMATION (From, To, By, Duration, BeginTime, AutoReverse, RepeatBehavior, FillBehavior, AccelerationRatio, DecelerationRatio, SpeedRatio)
	      (Storyboard.TargetName, Storyboard.TargetProperty)

MVVM