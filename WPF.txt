XAML

Binding (Source, ElementName, Path, BindingMode, UpdateSourceTrigger, TargetNullValue, RelativeSource, ValidationRules, ValidatesOnExceptions, ValidatesOnOataErrors, StringFormat) 
	(IValueConverter) 
	(CompositeCollection, MultiBinding, PriorityBinding)
	(привязка)

DependencyProperty (INotifyPropertyChnaged, DependencyObject) 
	(свойства зависимости)

RoutedEvent (маршрутизируемые события)

Trigger(триггер свойств, триггер данных, триггер события) 
	(Trigger, MultiTrigger, DataTrigger, MultiDataTrigger)

Command (ICommand)

Style (x:Key, TargetType)

Resources

Control(ContentControl, ItemsControl) 
	(однодетные элементы управления, многодетные элементы управления, диапазонные элементы управления, все остальное)

ResourceDictionary

Animation

MVVM